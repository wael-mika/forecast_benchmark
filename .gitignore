__pycache__/
*.py[cod]
.pytest_cache/
.DS_Store
.matplotlib/
.coverage
htmlcov/

# Virtual environment — recreate with requirements.txt on each machine
.venv/
venv/

# Model checkpoints — too large for git
**/*.pt
**/*.ckpt

# Raw and heavy processed data — too large or reproducible via scripts
data/raw/
data/processed/xgboost/
# Built feature frames (rebuild with scripts/prepare_features.py); the small
# split_boundaries.csv is tracked for reference.
data/processed/features/*.parquet

# Scratch outputs
tmp/

# Build metadata
src/*.egg-info/
