model_name: ann
enabled: true

feature_frame_path: data/processed/xgboost/features_weather_context_w14_h3.parquet
artifact_dir: artifacts/ann_weather_context_w14_h3

split_column: split

device: auto
loss_name: mse

batch_size: 4096
eval_batch_size: 8192
max_epochs: 25
early_stopping_patience: 6
checkpoint_interval: 5

learning_rate: 0.0005
weight_decay: 0.0001
lr_patience: 2
lr_decay_factor: 0.5
gradient_clip_norm: 1.0

embedding_dim: 16
hidden_dims: [512, 256, 128]
dropout: 0.15

seed: 42
