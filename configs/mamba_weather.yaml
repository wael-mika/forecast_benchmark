model_name: mamba
model_variant: advanced
enabled: true
split_column: split
device: auto
loss_name: trajectory
trajectory_point_loss: mse
early_stopping_metric: rmse
baseline_strategy: persistence
loss_horizon_weights:
- 1.0
- 1.2
- 1.45
loss_diff_weight: 0.45
loss_curvature_weight: 0.12
batch_size: 512
eval_batch_size: 1024
max_epochs: 150
early_stopping_patience: 25
checkpoint_interval: 10
train_fraction: 1.0
train_max_rows: 0
learning_rate: 0.001
min_learning_rate: 1.0e-05
weight_decay: 0.0001
scheduler_name: cosine
warmup_epochs: 10
lr_decay_factor: 0.5
lr_patience: 5
gradient_clip_norm: 1.0
min_sequence_coverage: 0.75
validation_eval_fraction: 1.0
validation_eval_max_rows: 0
test_eval_interval: 0
embedding_dim: 16
dropout: 0.1
seed: 42
artifact_dir: artifacts/mamba_weather
model_dim: 96
state_dim: 48
num_blocks: 3
kernel_size: 5
head_hidden_dim: 192
